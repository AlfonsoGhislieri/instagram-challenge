deleteComment = () => {
  let thingToDelete = document.getElementById("comment-<%=@comment.id%>");
  thingToDelete.className = "is-success notification is-light has-text-bold alert alert-success"
  thingToDelete.innerHTML = "";
  thingToDelete.innerText = "Comment deleted";

}

deleteComment();