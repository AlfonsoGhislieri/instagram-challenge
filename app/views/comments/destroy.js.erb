deleteComment = () => {
  let commentToDelete = document.getElementById("comment-<%=@comment.id%>");
  commentToDelete.className = "is-success notification is-light has-text-bold alert alert-success text-center offset-3 w-50 mt-1"
  commentToDelete.innerHTML = "";
  commentToDelete.innerText = "Comment deleted";
  commentToDelete.setAttribute("id", "flashmessage")
  
  setTimeout(() => deleteFlashMessages(), 1500)
}
deleteComment();